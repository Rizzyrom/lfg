# App Name
NEXT_PUBLIC_APP_NAME=LFG

# Database
DATABASE_URL="postgresql://user:password@host:5432/database"

# Upstash Redis
UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-token"

# n8n Webhook
N8N_WEBHOOK_URL="https://your-n8n-instance.app/webhook/your-secret"

# App Security
APP_WEBHOOK_SECRET="your-32-character-random-secret-here"
ALLOWED_ORIGINS="http://localhost:3000,https://yourdomain.com"

# Base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# WebSocket (optional)
WS_PORT=3001

# ========================================
# NEW: Chat Enhancements
# ========================================

# X (Twitter) API - Get from https://developer.twitter.com
# Required for: /whois command and auto-subscribe validation
# Optional but highly recommended
X_BEARER_TOKEN="AAAAAAAAAAAAAAAAAAAAAA%2FAAAAAAAAA%3DAAAAAAAAAAAAAAAAAAAAAAAAAAAA"

# AI/LLM Provider (choose one)
LLM_PROVIDER="openai"  # Options: "openai" or "anthropic"

# OpenAI (if using GPT-4)
OPENAI_API_KEY="sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# Anthropic (if using Claude)
ANTHROPIC_API_KEY="sk-ant-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# ========================================
# Notes
# ========================================
#
# X_BEARER_TOKEN:
#   - Get a Twitter Developer account: https://developer.twitter.com/en/portal/dashboard
#   - Create a project and app
#   - Generate a Bearer Token (not API Key/Secret)
#   - Used for validating @handles and auto-subscribe feature
#   - Without this, /whois will work but won't validate handles
#
# LLM Keys:
#   - Used for AI agent Q&A (@agent) and AI Pulse summaries
#   - OpenAI: https://platform.openai.com/api-keys
#   - Anthropic: https://console.anthropic.com/settings/keys
#   - Set LLM_PROVIDER to match your chosen provider
#
# Rate Limits:
#   - Commands: 10 per 5 minutes per user per group (in-memory)
#   - Upgrade to Redis-based rate limiting for production
